<!-- Main modal -->
<div id="defaultModal" tabindex="-1" 
     class="hidden overflow-y-auto fixed top-0 right-0 left-0 z-50 
            justify-center items-center w-full md:inset-0 h-modal md:h-full">
  <div class="relative p-4 w-full max-w-2xl h-full md:h-auto">
    <div class="relative p-4 bg-gray-800 rounded-lg shadow">
      <div class="flex justify-between items-center pb-4 mb-4 border-b border-gray-600">
        <h3 class="text-lg font-semibold text-white">Add Task</h3>
        <button type="button" class="text-gray-400 hover:bg-gray-600 
                                   hover:text-white rounded-lg p-1.5"
                data-modal-toggle="defaultModal">
          <svg aria-hidden="true" class="w-5 h-5" fill="currentColor" 
               viewBox="0 0 20 20">
            <path fill-rule="evenodd" 
                  d="M4.293 4.293a1 1 0 011.414 0L10 8.586l4.293-4.293a1 1 0 111.414 1.414L11.414 10l4.293 4.293a1 1 0 01-1.414 1.414L10 11.414l-4.293 4.293a1 1 0 01-1.414-1.414L8.586 10 4.293 5.707a1 1 0 010-1.414z" 
                  clip-rule="evenodd">
            </path>
          </svg>
        </button>
      </div>

      <!-- Modal body -->
      <form id="taskForm">
        <div class="grid gap-4 mb-4">
          <div>
            <label for="title" class="block mb-2 text-sm font-medium text-white">Title</label>
            <input type="text" id="title" name="title" 
                   class="bg-gray-700 border border-gray-600 text-white 
                          text-sm rounded-lg w-full p-2.5 focus:ring-primary-500 
                          focus:border-primary-500" required>
          </div>

          <div>
            <label for="description" class="block mb-2 text-sm font-medium text-white">Description</label>
            <textarea id="description" name="description" rows="4" 
                      class="bg-gray-700 border border-gray-600 text-white 
                             text-sm rounded-lg w-full p-2.5 focus:ring-primary-500 
                             focus:border-primary-500" required></textarea>
          </div>

          <div>
            <label for="priority" class="block mb-2 text-sm font-medium text-white">Priority</label>
            <select id="priority" name="priority" 
                    class="bg-gray-700 border border-gray-600 text-white 
                           text-sm rounded-lg w-full p-2.5 focus:ring-primary-500 
                           focus:border-primary-500">
              {% for priority in priorities %}
                <option value="{{ priority.id }}">{{ priority.id }}</option>
              {% endfor %}
            </select>
          </div>
        </div>

        <button type="submit" 
                class="text-white bg-primary-700 hover:bg-primary-800 
                       rounded-lg px-5 py-2.5 focus:ring-4 focus:outline-none 
                       focus:ring-primary-300">
          Add Task
        </button>
      </form>
    </div>
  </div>
</div>
